:::note
`File Contents` can be a reference to a binary file.
For example, if you have an `HTTP Get` action pull a .png image, you can reference `outputs.httpget.all` to write the .png to Google Cloud Storage.
Or, it can be simple text, like `'Hello World'`
:::
